{
  "name": "phr",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/phr/src",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rspack serve",
        "cwd": "apps/phr"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rspack build",
        "cwd": "apps/phr"
      },
      "dependsOn": ["^build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint src/",
        "cwd": "apps/phr"
      }
    }
  }
}
